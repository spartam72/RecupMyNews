<resources>

    <!-- Base application theme. -->
    <!--style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar"-->
    <style name="NavigationDrawer.Theme" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorMostPopular</item>
    </style>


    <!-- Date Picker Theme -->
    <style name="DatePickerTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/colorTopStories</item>
        <item name="colorControlActivated">@color/colorXanadu</item>
        <item name="colorControlHighlight">@color/colorText</item>
    </style>

    <!-- Dialog Theme -->
    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/colorXanadu</item>
    </style>


    <!-- Toolbar theme. -->
    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:fontFamily">@font/baumans</item>
    </style>

    <style name="SwitchCompatTheme" >
        <item name="colorControlActivated">@color/colorSport</item>
    </style>

</resources>
